int main(int ac, char **av)
{
	int x = atoi(av[1]);
	int n = 1;
	if (x <= 9)
	{
		char str[36]
		memcpy(str, argv, 4 * nb);
		if (x == 0x574f4c46)
			execl("/bin/sh", sh, NULL);
	}
	else
		return (1);
	return (0);
}

